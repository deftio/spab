# spab CLI — C++17 (skeleton)

Not yet implemented. A dependency-free, compiled command-line tool built against the C/C++ library
port (`src/c_cpp`), for environments without Node. Build with CMake, C++17.
