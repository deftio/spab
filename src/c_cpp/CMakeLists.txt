# spab — C/C++ port (skeleton; not yet implemented). C++17, no dependencies.
cmake_minimum_required(VERSION 3.15)
project(spab LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Library target (sources added when the port is implemented).
# add_library(spab STATIC src/spab.c)
# target_include_directories(spab PUBLIC include)

# No third-party dependencies by design.
