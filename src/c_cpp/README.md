# spab — C / C++ port (skeleton)

Not yet implemented. C++17, zero dependencies. A small C core (`include/spab.h`) with optional C++
conveniences; likely the performance/embeddable base with thin bindings for other languages.
Build with CMake. Validates against shared conformance vectors.
