# spab — Java / Kotlin port (skeleton)

Not yet implemented. JVM library, no dependencies (Kotlin Multiplatform is an option). Build with
Gradle. Mirrors `src/js/spab.js` and the frozen wire-format spec; validates against shared vectors.
