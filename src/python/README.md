# spab — Python port (skeleton)

Not yet implemented. Pure Python, no dependencies (no C-extension required). Import as `import spab`.
Mirrors `src/js/spab.js` and the frozen wire-format spec; validates against shared conformance vectors.
