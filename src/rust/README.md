# spab — Rust port (skeleton)

Not yet implemented. Will mirror `src/js/spab.js` and the frozen wire-format spec, dependency-free
(`[dependencies]` stays empty). Build: `cargo build`. Validates against shared conformance vectors.
