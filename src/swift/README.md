# spab — Swift port (skeleton)

Not yet implemented. SwiftPM package, zero dependencies; targets Apple platforms and Linux via the
open-source Swift toolchain. Build: `swift build`. Mirrors `src/js/spab.js` and the frozen
wire-format spec; validates against the shared conformance vectors.
